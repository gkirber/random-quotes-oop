const config = {
  // Quotable (api.quotable.io) часто недоступний через DNS/простої — ZenQuotes стабільніший.
  PUBLIC_API_URL: 'https://zenquotes.io/api',
  API_URL: 'https://random-quotes-oop-2yx3.onrender.com',
};

export default config;